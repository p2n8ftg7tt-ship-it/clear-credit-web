---

description: "Task list for Floating Rate Houses (Buy-a-House page)"
---

# Tasks: Floating Rate Houses (Buy-a-House page)

**Input**: Design documents from `/specs/006-floating-rate-houses/`

**Prerequisites**: [plan.md](./plan.md), [spec.md](./spec.md), [research.md](./research.md), [data-model.md](./data-model.md), [contracts/](./contracts/), [quickstart.md](./quickstart.md)

**Tests**: INCLUDED. The constitution (Flujo de desarrollo → Pruebas; Principio IV) requires `node --test tests/` coverage for rule-based logic, and plan.md lists five extended test files. Tests in each story are written **first** and must fail before the implementation makes them pass. A failing test is reported as is; a test is never weakened to pass.

**Organization**: Grouped by user story (US1–US4 from spec.md). US1 is the MVP; US2 is also P1 and should follow immediately; US3 and US4 are P2.

## Format: `[ID] [P?] [Story] Description`

- **[P]**: can run in parallel (different files, no dependency on an unfinished task)
- **[Story]**: US1…US4, mapped to the user stories in spec.md
- All paths are relative to the repository root `C:\Users\drcor\Desktop\MyWeb\` (flat static site; there is no `src/`). Note the two files named `tasas-hipoteca.js`: the **root** one is browser code, the one under `netlify/functions/` is the public function.

## Global rules for every task

- Feature 004's files (`tasas-hipoteca.js`, `tasas-texto.js`, `netlify/functions/tasas-*.js`, `tests/tasas-*.test.js`) already exist and are **untracked in git** — edit them in place and keep every existing test passing.
- Keep **LF** line endings. These files are untracked, so `git diff --stat` shows nothing for them: after editing, run `grep -c $'\r' <file>` and confirm `0`.
- No new dependency, framework, build step, image/icon asset or external browser origin. The Content-Security-Policy in `netlify.toml` stays unchanged.
- The houses are drawn **only by script** inside the existing `TASAS_LANZADO` guard in `tasas-hipoteca.js`. `comprar-casa.html` gets **CSS only** — no houses markup and **no typed rate number** anywhere in the HTML, CSS or JS source.
- Every DOM write uses `createElement` + `textContent` (never HTML built from data). Every `sessionStorage` access is inside `try/catch`. Nothing may throw to the page.
- All wording follows [contracts/ui-houses.md](./contracts/ui-houses.md) §5: state only what a figure is, when it was published, what changed and where it comes from; never "en vivo", "en tiempo real", advice, prediction or urgency; lenders appear only as unranked links, never with figures.
- Text shown to people is Spanish (es-US); code comments follow the surrounding file (Spanish).
- Do **not** publish or deploy from these tasks, do **not** set `tasas_config.activo = true`, do **not** change `TASAS_LANZADO` to `true` in the committed file. Launch is an owner action (last phase) and rides inside feature 004's launch commit.
- Numbers used in tests and fixtures (6.95, 6.26, …) are illustrative examples only.

---

## Phase 1: Setup

**Purpose**: known-good baseline before touching anything.

- [X] T001 Run `node --test tests/` from the repo root and record the result. If anything already fails, stop and report it before changing code.
- [X] T002 [P] Run `graphify query "tasas-hipoteca pintarBloque, comprar-casa rate watch section, credit-coach launcher"` and skim the result so the files you touch match the graph; note any file in it that the plan does not list.

---

## Phase 2: Foundational (blocking prerequisites)

**Purpose**: shared text constants, test fixtures, the local preview server and the extended fake-DOM harness that every story uses.

**⚠️ CRITICAL**: no user story work can begin until this phase is complete.

- [X] T003 [P] Create `tests/fixtures/tasas/snapshot-casas.json`: an object keyed by scenario name, each value `{ "estado": <http status>, "cuerpo": <snapshot object or null> }`, following [contracts/snapshot-additive.md](./contracts/snapshot-additive.md) and the 004 snapshot shape (`version`, `activo`, `frescura`, `publicadoEn`, `proximaActualizacion`, `fuente`, `terminos["30"|"15"]` with `valor, previo, cambioPp, fechaFuente`, `alertas`, `fuentesSenal`, plus `ultimaRevisionEn`). Scenarios (exact keys): `al-dia` (fresh, no alerts, `ultimaRevisionEn` = the string `"__AHORA_MENOS_2H__"`), `alerta-30` (one `movimiento_semanal` alert with `termino: "30"`), `alerta-fed` (one `fed_objetivo` alert with `termino: null`, `semanalPendiente: true`, `datos: { desde, hasta }`), `sin-actualizar` (`frescura: "sin_actualizar"`), `revision-vieja` (`frescura: "al_dia"`, `ultimaRevisionEn` = `"__AHORA_MENOS_72H__"`), `sin-datos` (`frescura: "sin_datos"`, `terminos: null`, `alertas: []`), `caido` (`estado: 503`, `cuerpo: { "estado": "no_disponible" }`). The two placeholder strings are replaced by the preview server (T004) with real ISO times.
- [X] T004 Create `tests/preview-casas.js` (Node built-ins only, `'use strict'`, header comment: throwaway local check, never deployed — `tests/` is already blocked by a 404 rule): an `http` server on port `8888` that serves files from the repo root with correct content types (`.html .js .css .json .png .svg .webp`); when a request is `/comprar-casa.html?caso=<name>` it stores `<name>` as the current scenario (default `al-dia`); it answers `GET /.netlify/functions/tasas-hipoteca` from `tests/fixtures/tasas/snapshot-casas.json` for the current scenario (status from `estado`, JSON body from `cuerpo`, `Cache-Control: no-store`), replacing `"__AHORA_MENOS_2H__"` / `"__AHORA_MENOS_72H__"` with ISO times computed from `Date.now()`; when it serves `/tasas-hipoteca.js` it rewrites, **in the response only**, `const TASAS_LANZADO = false;` → `true`, and if the page URL had `?refresco=<minutes>` also rewrites `const REFRESCO_MINUTOS = 15;` → that number (the file on disk is never changed). Print the base URL on start. Depends on T003.
- [X] T005 [P] In `tasas-texto.js` extend `TEXTOS` with a `casa` group (all Spanish, no digits typed as rates): `grupo` "Tasas hipotecarias de referencia", `plazo30` "30 años", `plazo15` "15 años", `estadoActivo` "Activo", `estadoSinActualizar` "Sin actualizar", `aviso` "Aviso", `cerrar` "Ocultar las casas de tasas", `abrir` "Abrir detalles", `cerrarPanel` "Cerrar detalles", `ultimaRevision` "Última revisión del agente", `verDetalleCompleto` "Ver el detalle completo", `fuentesTitulo` "De dónde salen los números", `prestamistasTitulo` "Ver las tasas de cada prestamista", `prestamistasAviso` "Estas páginas muestran las tasas propias de cada prestamista, que varían según la persona.". Also add to `PALABRAS_PROHIBIDAS` the four strings `en vivo`, `en tiempo real`, `live`, `real time`, and export `REVISION_ATRASADA_HORAS = 48` (data-model.md: "48"). Do not change any existing string or export.
- [X] T006 [P] Extend `tests/tasas-texto.test.js`: assert `PALABRAS_PROHIBIDAS` contains the four new strings, `contieneProhibida('en vivo')`/`('tasa en tiempo real')`/`('live')`/`('real time')` each return non-null, and every string inside `TEXTOS.casa` passes `contieneProhibida(...) === null`; assert `REVISION_ATRASADA_HORAS === 48`. Then run `node --test tests/tasas-texto.test.js` and confirm all existing tests still pass with the enlarged list.
- [X] T007 Extend the fake-DOM harness in `tests/tasas-navegador.test.js` **without changing what existing tests see**: give `El` a `tagName` getter (upper-case of `tag`), `contains(x)`, `removeAttribute(k)`, and make `setAttribute('hidden', …)` unnecessary (keep `hidden` as a property); extend `correr()` with optional inputs `sesion` (object backing a `sessionStorage` with `getItem/setItem`, same style as `almacenamiento`), `sesionRota` (throws on access), `dialogos` (list of `El` returned by `document.querySelectorAll('.fha-world')`, default `[]`), and `visibilidad` (initial `document.visibilityState`, default `'visible'`); make `document.addEventListener` store listeners so a test can fire `focusin`/`focusout`/`visibilitychange`/`keydown`; add a fake `setInterval`/`clearInterval` that records callbacks and delays (returned in the result as `intervalos`) so a test can trigger a refresh by calling the callback; add an optional `MutationObserver` stub that records its callback and observed nodes (returned as `observadores`); make `fetchFalso` accept a list of successive responses (`respuestas`) in addition to the single `respuesta`. Return `{ …existing, sesion, intervalos, observadores, doc }`. Run `node --test tests/tasas-navegador.test.js` and confirm every existing test still passes.

**Checkpoint**: text constants, fixtures, preview server and harness are ready; user stories can begin.

---

## Phase 3: User Story 1 — See both rates at a glance on two floating houses (Priority: P1) 🎯 MVP

**Goal**: two house icons, 30 años and 15 años, float on Comprar casa and show each interest rate as a visible number; small, in the bottom-left corner, out of the way of the assistant, hidden while typing or under a full-screen tool, and dismissible for the visit. Rates come from the same snapshot object as the fixed block.

**Independent Test**: with the preview server (`node tests/preview-casas.js`, `?caso=al-dia`) confirm two houses show a term and a rate without any tap, stay in the corner during a full scroll, do not cover the assistant launcher, hide while a form field has focus, and can be dismissed (quickstart §3 items 1, 3, 4, 8, 9, 10). Automated: T008–T010 pass.

### Tests for User Story 1 (write first; they must FAIL before implementation)

- [X] T008 [P] [US1] In `tests/tasas-texto.test.js` add cases for `textoCasa(termino, dato)`: for `('30', { valor: 6.95, previo: 6.76, cambioPp: 0.19, fechaFuente: '2026-09-17' })` it returns `{ plazo: '30 años', valor: '6.95 %', cambio: '▲ sube 0.19 puntos porcentuales', fecha: 'publicado por Freddie Mac el 17 sep', nombreAccesible }` where `valor`, `cambio` and `fecha` are **strictly equal** to `textoCifra(...).valor`, `textoCambio(...)` and `textoFechaFuente(...)` (single truth, FR-016); `nombreAccesible` starts "Hipoteca fija a 30 años: 6.95 %," and ends "Abrir detalles."; for `'15'` the plazo is "15 años"; for `cambioPp: 0` the cambio is "= sin cambio"; every returned string passes `contieneProhibida(...) === null`.
- [X] T009 [P] [US1] In `tests/tasas-navegador.test.js` add houses cases using `conLanzamiento` and `snapshot()`: (a) `codigoReal` (pre-launch) creates no houses and makes no request; (b) launched on a page with `#tasas` → `document.body` gains one child with `className === 'casas-flotantes'` and `role="group"`, containing two `.casa` buttons (`data-termino` `'30'` then `'15'`) whose `.casa-valor` text is `'6.95 %'` and `'6.26 %'` and `.casa-plazo` text "30 años"/"15 años", and `body` has attribute `data-casas="1"`; (c) their `.casa-valor` text equals `porId.tasas30Valor.textContent` / `tasas15Valor` (block and houses agree); (d) launched on a page **without** `#tasas` → no houses (only the existing banner path); (e) `activo: false` → no houses; (f) `frescura: 'sin_datos'` with `terminos: null` → no houses; (g) `terminos` with only `'30'` → exactly one house; (h) fetch failure (`falla: true`) → no houses and no exception; (i) focus: firing `focusin` with target `{ tagName: 'INPUT' }` sets the group `hidden = true`, `focusout` restores it; `focusin` on a `BUTTON` does not hide it; (j) dialogs: with a `dialogos` element whose `hidden` is `false` the group starts hidden, and invoking the recorded `MutationObserver` callback after setting it `true` shows the group again; with no `MutationObserver` in the context nothing throws; (k) dismiss: clicking `.casas-cerrar` removes/hides the group, writes `themora_casas_cerradas = '1'` to `sesion`, fires event `tasas-casas-cerradas` **without** properties, and a second run with `sesion = { themora_casas_cerradas: '1' }` draws no houses; with `sesionRota: true` dismissal still hides the group for that page view and nothing throws; (l) `tasas-casa-vista` fires once per drawn term with properties exactly `{ termino: '30' }` / `{ termino: '15' }`.
- [X] T010 [P] [US1] In `tests/tasas-paginas.test.js` (static file reads): assert `comprar-casa.html` defines `.casas-flotantes` with `position:fixed` and a `z-index` **lower than 200** (assistant launcher) and equal to 140, sits at `bottom` and `left`, and that any `animation` on `.casa` appears only inside an `@media (prefers-reduced-motion: no-preference)` block; assert the HTML contains **no element** with class `casas-flotantes` or `casa` (CSS only), and **no typed rate number** (`\d+\.\d+ ?%`) in the houses CSS or in the houses code of `tasas-hipoteca.js`; **update the analytics allowlist test** (`la analítica del bloque usa solo eventos categóricos…`): add `tasas-casa-vista`, `tasas-casa-abierta`, `tasas-casas-cerradas` to the allowed event names and allow the property shape `{ termino: <identifier> }` in addition to `{ tipo: <identifier> }` (still nothing else).

### Implementation for User Story 1

- [X] T011 [US1] In `tasas-texto.js` add and export `textoCasa(termino, dato)` exactly as specified by T008 (compose `plazo` from `TEXTOS.casa.plazo30|plazo15`, reuse `textoCifra`, `textoCambio`, `textoFechaFuente` — do not reformat numbers or dates; `nombreAccesible` = `Hipoteca fija a {N} años: {valor}, {cambio}, {fecha}. Abrir detalles.`, with `cambio` exactly the block's text including its ▲/▼ glyph). No estado or marca yet (added in US2).
- [X] T012 [US1] In `comprar-casa.html`, append to the existing `<style>` block that already holds the `.tasas-*` rules (around line 54–76) the CSS for `.casas-flotantes` and its parts, using the page tokens (`--navy-deep`, `--gold`, `--gold-light`, `--paper-dim`): group `position:fixed; left:16px; bottom:16px; z-index:140; display:flex; gap:8px; align-items:flex-end` (12px offsets and side-by-side compact sizing ≤ 560px; stacked column ≥ 900px); `.casas-flotantes[hidden]{display:none}`; `.casa` as a `<button>` reset with `clip-path:polygon(50% 0,100% 40%,100% 100%,0 100%,0 40%)`, navy background, `.casa-plazo` small, `.casa-valor` gold-light, ≈ 88×80px on phones, `cursor:pointer`, `:focus-visible` visible outline (a `clip-path` clips the outline — draw the focus ring with a filter/drop-shadow or an inner `box-shadow inset`, and check it is visible), `.casas-cerrar` small round "×" button; `body[data-casas]{padding-bottom:110px}` so the last content is never trapped; the gentle float `@keyframes` and `animation` **only** inside `@media (prefers-reduced-motion:no-preference)`. No houses markup and no typed rate. Must satisfy T010.
- [X] T013 [US1] In `tasas-hipoteca.js` (inside the existing IIFE, **after** the `TASAS_LANZADO` guard) add `pintarCasas(datos)`: only when the page has `#tasas`; remove/skip drawing unless `datos.activo === true`, `datos.frescura !== 'sin_datos'` and `datos.terminos`; create once (then update in place on later calls) a `div.casas-flotantes` with `role="group"` and `aria-label` = `T.TEXTOS.casa.grupo`, appended to `document.body`, containing for each term present, in order `'30'` then `'15'`, a `<button type="button" class="casa" data-termino>` with children `.casa-plazo`, `.casa-valor` (from `T.textoCasa`), `aria-label` = `nombreAccesible`, `aria-expanded="false"`, `aria-controls="casasPanel"`; a `<button class="casas-cerrar">` with `aria-label` = `T.TEXTOS.casa.cerrar`; set `document.body` attribute `data-casas="1"` while the group is shown and remove it when not. Call `evento('tasas-casa-vista', { termino })` once per term per page view. Call `pintarCasas(datos)` from the success path of `pintarBloque` (and remove the houses when `pintarBloque` decides the block stays hidden or unavailable). Use only `createElement`/`textContent`; wrap so nothing throws.
- [X] T014 [US1] In `tasas-hipoteca.js` add the behaviors around the group (same file, after T013): (a) **hide while typing** — `document.addEventListener('focusin', …)` hides the group when `e.target.tagName` is `INPUT`, `SELECT` or `TEXTAREA`, and `focusout` restores it; (b) **hide under a full-screen tool** — collect `document.querySelectorAll('.fha-world')` (the dialogs `#fhaWorld` and `#convWorld`), hide the group while any has `hidden === false`, re-evaluate through a `MutationObserver` on their `hidden` attribute when `typeof MutationObserver === 'function'` (otherwise do nothing: the tool's `z-index:300` already covers the group); (c) **dismiss** — the `.casas-cerrar` click hides the group, stores `themora_casas_cerradas` = `"1"` in `sessionStorage` (read/write each inside `try/catch`), calls `evento('tasas-casas-cerradas')` with no properties, and a stored `"1"` at start means the houses are not drawn (with blocked storage the group only stays hidden for the current page view). Temporary hiding (a, b) must not be confused with dismissal (c).
- [ ] T015 [US1] Run `node --test tests/` and make T008–T010 pass; then open the preview (`node tests/preview-casas.js`, `http://localhost:8888/comprar-casa.html?caso=al-dia`) at 360 px and 1280 px and confirm quickstart §3 items 1, 3, 4, 8, 9 and 10. Fix anything that fails in the code, not the test.
  - _Estado: parte automática hecha (301/301 pruebas, servidor de vista previa verificado con curl). Falta la revisión visual en navegador a 360 px y 1280 px (quickstart §3, ítems 1, 3, 4, 8, 9 y 10)._

**Checkpoint**: US1 is fully functional and independently testable — two houses with visible rates, in place, hideable and dismissible.

---

## Phase 4: User Story 2 — The houses are the "active agents": live status, freshness and change (Priority: P1)

**Goal**: each house shows "Activo" only when its figure is fresh **and** the agent has really checked recently, else "Sin actualizar" with the last verified figure; shows an "Aviso" mark when the rate agent has an applicable alert; and picks up newer readings without a reload.

**Independent Test**: with the preview, `?caso=al-dia` → "Activo"; `?caso=sin-actualizar` and `?caso=revision-vieja` → "Sin actualizar" (never "Activo"); `?caso=alerta-30` → only the 30-year house marked; `?caso=alerta-fed` → both marked and house numbers unchanged (quickstart §2 and §3 item 14). Automated: T016–T019 pass.

### Tests for User Story 2 (write first; they must FAIL before implementation)

- [ ] T016 [P] [US2] In `tests/tasas-hipoteca-logica.test.js` add: `construirRespuestaPublica({ config: { activo: true }, publicado, alertas: [], ahora, ultimaRevision: '2026-09-24T13:00:04.000Z' })` returns a body whose `ultimaRevisionEn` is exactly that ISO string; with `ultimaRevision` `null` or omitted it is `null`; the body never gains any other key from a run row (compare the key set with the current 004 key set plus only `ultimaRevisionEn`); `version` is still `1`.
- [ ] T017 [P] [US2] In `tests/tasas-hipoteca-funcion.test.js` add (using the existing `deps` injection style): the handler issues a read whose URL contains `tasas_corridas`, `resultado=in.(ok,parcial)`, `order=corrida_en.desc` and `limit=1`; the response `ultimaRevisionEn` equals that row's `corrida_en`; when **only** the runs read fails (`ok: false` / throws) the response is still `200` with `ultimaRevisionEn: null` and the other fields intact; when the config read or the published-snapshot read fails the response is still `503 { estado: 'no_disponible' }` (unchanged); the `activo: false` response is exactly `{ version: 1, activo: false }` and makes no runs read; the body never contains `tipo`, `fuentes`, `duracion_ms`, `retenidas` or `resultado` from the runs table.
- [ ] T018 [P] [US2] In `tests/tasas-texto.test.js` add cases: `estadoCasa(datos, ahora)` returns `'activo'` for `frescura 'al_dia'` with `ultimaRevisionEn` 2 h before `ahora`; `'activo'` when `ultimaRevisionEn` is missing/`null` (no claim beyond `frescura`); `'sin_actualizar'` when `ultimaRevisionEn` is 72 h old; `'sin_actualizar'` at exactly 48 h (boundary: **less than** 48 h is active); `'sin_actualizar'` for `frescura 'sin_actualizar'` even with a fresh check; `marcaDeAlerta(datos, '30')` is true for an alert with `termino '30'`, false for one with `termino '15'`, true for both terms when the alert has `termino: null` (Treasury/Fed), false with `alertas: []` or missing; `textoCasa` now also returns `estado` text ("Activo"/"Sin actualizar"), `marca` ("Aviso" or `''`) and the accessible name gains "Estado: activo." / "Estado: sin actualizar." and "Hay un aviso." only when marked; all returned strings still pass `contieneProhibida(...) === null`.
- [ ] T019 [P] [US2] In `tests/tasas-navegador.test.js` add cases: fresh snapshot with `ultimaRevisionEn` 2 h old → both `.casa-estado` texts "Activo" and both `.casa-marca` hidden; `frescura: 'sin_actualizar'` → "Sin actualizar" and the figures/dates are still those of the snapshot (no invented number); `ultimaRevisionEn` 72 h old with `frescura: 'al_dia'` → "Sin actualizar"; missing `ultimaRevisionEn` → "Activo" only if `frescura` is `al_dia`; an alert with `termino '30'` → only the 30-year `.casa-marca` visible with text "Aviso"; a Treasury/Fed alert (`termino: null`) → both visible **and the two `.casa-valor` texts unchanged** (a signal never moves the headline); **refresh**: the recorded `setInterval` callback exists with a delay of `15 * 60 * 1000` ms (`REFRESCO_MINUTOS = 15`), invoking it with a second response (`respuestas`) that has new values updates both houses **and** the fixed block's figures to the new values; a failing second response leaves the last drawn figures in place and throws nothing; with `visibilidad: 'hidden'` invoking the callback makes no request; firing `visibilitychange` with `visible` after more than 15 minutes triggers one request.

### Implementation for User Story 2

- [ ] T020 [US2] In `netlify/functions/tasas-hipoteca-logica.js` extend `construirRespuestaPublica({ config, publicado, alertas, ahora, ultimaRevision })` to add `ultimaRevisionEn: ultimaRevision || null` to the `activo: true` body (an ISO string or `null`, nothing else); keep `version: 1` and every existing field/ordering; update the header comment. Do not add any other run data.
- [ ] T021 [US2] In `netlify/functions/tasas-hipoteca.js` add, after the config check says `activo` is true, one more read with the existing `leer` helper: `tasas_corridas?resultado=in.(ok,parcial)&order=corrida_en.desc&limit=1&select=corrida_en`, run **in parallel** with the alerts read, wrapped so a failure of only this read yields `null` (`.catch(() => [])` semantics) and does not fail the response; pass `ultimaRevision: filas[0] ? filas[0].corrida_en : null` into `L.construirRespuestaPublica`. Do not change the cache header, the `activo: false` path or the 503 paths. Update the header comment listing what the function reads.
- [ ] T022 [US2] In `tasas-texto.js` add and export `estadoCasa(datos, ahora)`, `marcaDeAlerta(datos, termino)` and extend `textoCasa(termino, dato, contexto)` (third argument `{ estado, marca }`, optional and backward compatible with T011/T008) exactly as specified by T018 (`estadoCasa` uses `REVISION_ATRASADA_HORAS` from T005: active only if `frescura === 'al_dia'` **and** (`ultimaRevisionEn` is missing/`null` **or** age **< 48 h**); `sin_datos` never reaches a house). Pure functions, no `Date.now()` inside (take `ahora` as a `Date` argument).
- [ ] T023 [P] [US2] In `comprar-casa.html` add CSS for `.casa-estado` (small status line inside the house, text not color alone; a distinct style for `data-estado="sin_actualizar"` such as muted gold/paper) and `.casa-marca` (small "Aviso" badge with a border/shape, `hidden` respected). Keep the reduced-motion rule from T012 covering any pulse; no new animation outside `prefers-reduced-motion:no-preference`.
- [ ] T024 [US2] In `tasas-hipoteca.js` extend `pintarCasas(datos)`: compute `estado = T.estadoCasa(datos, new Date())` and `marca = T.marcaDeAlerta(datos, termino)` per house, fill `.casa-estado` (`data-estado` attribute = `activo` | `sin_actualizar`) and `.casa-marca` (`hidden` unless `marca`), pass `{ estado, marca }` to `T.textoCasa` so the `aria-label` is updated on every draw; the figure/date shown for a stale house are still the snapshot's own (never estimated).
- [ ] T025 [US2] In `tasas-hipoteca.js` add the live refresh: `const REFRESCO_MINUTOS = 15;` (exact line, so T004's preview rewrite works); after the first successful render start `setInterval(refrescar, REFRESCO_MINUTOS * 60 * 1000)` where `refrescar` does nothing when `document.visibilityState !== 'visible'`, otherwise re-runs the single `pedir()` and feeds **the same** result to `pintarBloque` and `pintarCasas`; a failed refresh changes nothing and throws nothing; listen to `visibilitychange` and, when the tab becomes visible and more than `REFRESCO_MINUTOS` minutes have passed since the last successful fetch, refresh once. Only on the Comprar casa page (`#tasas` present); never on banner-only pages.
- [ ] T026 [US2] Run `node --test tests/` and make T016–T019 pass; then in the preview walk quickstart §2 scenarios `al-dia`, `sin-actualizar`, `revision-vieja`, `alerta-30`, `alerta-fed` and §3 item 14 (`?caso=al-dia&refresco=0.1`, then change the served scenario by opening the page again with another `caso` and confirm the running tab updates on its next poll). Fix anything that fails in the code, not the test.

**Checkpoint**: US1 + US2 work together — the houses are honest, live-ish agents.

---

## Phase 5: User Story 3 — Tap a house to see where its rate comes from (Priority: P2)

**Goal**: activating a house opens a shared, non-modal details panel with the term, rate, change, date, last agent check, applicable alerts, the sources (what each measures, how often, link), the Federal Reserve note, links to the big lenders' own rate pages (no lender figures), the reference-only disclaimer and a link to the full block; keyboard- and screen-reader-operable.

**Independent Test**: with the preview (`?caso=al-dia`, `?caso=alerta-fed`) tap each house; read the panel; open and close it by mouse and by keyboard only (Enter/Space/Escape), with focus returning to the house (quickstart §3 items 5, 6, 7, 11). Automated: T027–T029 pass.

### Tests for User Story 3 (write first; they must FAIL before implementation)

- [ ] T027 [P] [US3] In `tests/tasas-texto.test.js` add: **lender list rules** on `PRESTAMISTAS` — every entry has a non-empty `nombre` and a `url` that starts with `https://`, contains no `?` and no `#`, urls are unique, names are unique, the list is sorted alphabetically by `nombre` (no ranking), no `nombre` matches `\d\s?%`, and none of the names/notice contain `mejor`, `recomend`, `aprobad`, `oficial de`, or any forbidden word; `textoPanelCasa('30', datos, ahora)` returns structured sections in this order — `titulo` ("Hipoteca fija a 30 años"), `cifras` (same `valor`/`cambio`/`fecha` strings as `textoCasa`), `revision` (starts "Última revisión del agente:" and only present when `ultimaRevisionEn` is known, empty otherwise), `alertas` (the `textoAlerta(...)` text of alerts that apply to that term, empty array if none, each with the source `url`), `fuentes` (from `textoFuentes([datos.fuente].concat(datos.fuentesSenal))`), `notaFed` (the existing Fed wording "Las tasas hipotecarias no las fija directamente la Reserva Federal…"), `prestamistas` (`{ titulo, aviso, enlaces }`, `enlaces` = `PRESTAMISTAS` and the whole section `null` when the list is empty), `avisoLegal` (strictly equal to `TEXTOS.avisoLegal`) and `enlaceCompleto` ("#tasas"); serialize every generated string in the panel and assert `contieneProhibida(...) === null`, that none of them contains "en vivo", "en tiempo real", and that the lenders section contains no digit-percent pattern.
- [ ] T028 [P] [US3] In `tests/tasas-navegador.test.js` add cases (launched, `snapshot()`): the group contains one `div#casasPanel` (`role="region"`, `aria-labelledby` → its title id, `hidden` initially); clicking house `'30'` makes the panel visible, sets that house's `aria-expanded="true"` (the other `"false"`), fills the title "Hipoteca fija a 30 años", shows the source list with an external link per source (`target="_blank"`, `rel="noopener noreferrer"`), the Fed note, the disclaimer text strictly equal to `T.TEXTOS.avisoLegal`, and a link to `#tasas`; clicking house `'15'` while open switches the content (still one panel, `aria-expanded` moves); clicking the same house again closes it; firing `keydown` with `{ key: 'Escape' }` closes it, sets `aria-expanded="false"` and calls `.focus()` on the house that opened it; clicking `cerrarPanel` closes it; an alert (`alertas: [alerta()]`) is shown inside the panel with its source link and the block's `textoAlerta` text; with the code run against an empty `PRESTAMISTAS` (replace the array in the executed text, not in the file) no lenders section is drawn; with a non-empty list the lender links are drawn in list order with `data-umami-event="tasas-prestamista-abierto"` and **no** `?` in any `href`; opening fires `tasas-casa-abierta` with properties exactly `{ termino }` and nothing else; a missing `ultimaRevisionEn` draws no "Última revisión" line; nothing throws when `sesionRota: true`.
- [ ] T029 [P] [US3] In `tests/tasas-paginas.test.js` add: the analytics allowlist now also accepts `tasas-casa-abierta` (already added in T010) and the `data-umami-event` value `tasas-prestamista-abierto` (assert it appears in the source and no lender **name** is passed as an event property); assert `tasas-hipoteca.js` never assigns lender rates (no digit-percent inside the panel code); **launch guard (self-updating)**: read `TASAS_LANZADO` from `tasas-hipoteca.js` — while `false`, no assertion about the lender list; once `true`, `PRESTAMISTAS` (loaded from `tasas-texto.js`) MUST have at least one entry, so a launch cannot ship the panel without the owner-approved links (G-LENDERS).

### Implementation for User Story 3

- [ ] T030 [US3] In `tasas-texto.js` add and export `PRESTAMISTAS` and `textoPanelCasa(termino, datos, ahora)` as specified by T027. `PRESTAMISTAS` ships as **`[]` with a header comment** stating that the owner must approve the list and open each link by hand before launch (G-LENDERS), with the candidate names from research.md R7 (Bank of America, Chase, loanDepot, Rocket Mortgage, U.S. Bank, Wells Fargo) written **only in the comment, without URLs**; entry shape `{ nombre, url }`; rules from data-model.md quoted verbatim in the comment: "`url` starts with `https://`, no query string or fragment, unique; `nombre` non-empty, no digits followed by `%`; alphabetical by `nombre`". Export a small `validarPrestamistas(lista)` returning a list of problems (used by T027) so the rules live in one place. `textoPanelCasa` reuses `textoAlerta`, `textoFuentes`, `textoCasa` and the existing `FED_NO_FIJA` wording (expose it if it is not exported yet); it must never build a lender figure.
- [ ] T031 [P] [US3] In `comprar-casa.html` add CSS for `.casas-panel` (non-modal card anchored above the group: `position:absolute; bottom:calc(100% + 10px); left:0; width:min(360px,calc(100vw - 24px)); max-height:min(70vh,460px); overflow:auto; background:#fff; border:1px solid var(--line); border-radius:14px; box-shadow:var(--shadow); padding:16px`; `[hidden]{display:none}`), its title, source list, alert cards (reuse `.tasas-alerta`), lender links list, close button and the "Ver el detalle completo" link; it must not cause horizontal scroll at 360 px and must sit inside the group's stacking context (below the assistant `z-index:200`).
- [ ] T032 [US3] In `tasas-hipoteca.js` add the panel: create the `div#casasPanel` (`role="region"`, `aria-labelledby="casasPanelTitulo"`, `hidden`) inside the group; house click/Enter/Space (`<button>` gives the keys natively) → `abrirPanel(termino, boton)`: build the content from `T.textoPanelCasa(termino, datosActuales, new Date())` with `createElement`/`textContent` only, source and lender links as `<a target="_blank" rel="noopener noreferrer">` reusing the existing `enlaceExterno` helper with events `tasas-fuente-abierta` and `tasas-prestamista-abierto`, set `aria-expanded` on the houses, remember the opener button, call `evento('tasas-casa-abierta', { termino })`; opening the other house switches content; activating the same house, the panel's close button, or a document `keydown` with `Escape` closes it, resets `aria-expanded="false"` and returns focus with `.focus()` to the opener; the panel content is rebuilt (not left stale) when the refresh from T025 delivers new data while it is open; the panel closes when the group is hidden or dismissed. Keep `datosActuales` as the single snapshot object shared with the block and the houses.
- [ ] T033 [US3] Run `node --test tests/` and make T027–T029 pass; then in the preview use only the keyboard to open/close both houses and read the panel with a screen reader (quickstart §3 items 5–7, 11), and confirm at 360 px that the panel fits and scrolls inside itself. Fix anything that fails in the code, not the test.

**Checkpoint**: US1–US3 work independently and together.

---

## Phase 6: User Story 4 — The houses follow the same launch gate and the same data as the block (Priority: P2)

**Goal**: nothing appears before launch; after launch the houses and the fixed block always show identical rate, change, date and source; switching the feature off (`activo: false`) removes everything.

**Independent Test**: with `TASAS_LANZADO = false` no houses and no request (automated); with the preview, change the served scenario and confirm block and houses agree (quickstart §3 items 2, 12) and that `?caso=sin-datos` / `?caso=caido` show no houses while the block says so honestly. Automated: T034–T035 pass.

### Tests for User Story 4 (write first)

- [ ] T034 [P] [US4] In `tests/tasas-paginas.test.js` add: the houses code in `tasas-hipoteca.js` sits **after** the `TASAS_LANZADO !== true` guard (assert the index of the first `casas-flotantes` / `pintarCasas` in the source is greater than the index of the guard); `comprar-casa.html` loads `tasas-texto.js` **before** `tasas-hipoteca.js` (both `defer`); **self-updating privacy guard**: while `TASAS_LANZADO` is `false`, `privacidad.html` must NOT contain the phrase "ocultar las casas de tasas"; once `true` it MUST contain it (the launch commit widens the existing rate-notice line — see T044); keep the existing pre-launch cleanliness test unchanged and passing.
- [ ] T035 [P] [US4] In `tests/tasas-navegador.test.js` add: for one `datos` object, every house's `.casa-valor`, and the text in the block (`tasas30Valor`/`tasas15Valor`, `tasas30Cambio`/`tasas15Cambio`, `tasas30Fecha`/`tasas15Fecha`) agree with `T.textoCasa` output character for character (`valor`, `cambio`, `fecha`); when a refresh (recorded `setInterval` callback, second response) returns `activo: false` the houses **and** the panel are removed/hidden and `body` loses `data-casas`, and the block hides; when the refresh returns `frescura: 'sin_datos'` the houses disappear; when it returns a snapshot with different figures both the houses and the block change together (no case where one changed and the other did not); the test run makes exactly one request per refresh (block and houses share it).

### Implementation for User Story 4

- [ ] T036 [US4] In `tasas-hipoteca.js` make `pintarCasas`, the panel and the refresh use **one** shared `datosActuales` and one entry function (for example `mostrar(datos)` that calls `pintarBloque(seccion, datos)` and `pintarCasas(datos)` together) so there is exactly one code path; on `activo !== true` or `sin_datos` remove the group, close the panel, remove `data-casas` from `body`; keep the refresh interval running in that case (the owner may switch the feature back on) but draw nothing. Verify T035 passes.
- [ ] T037 [US4] Run `node --test tests/` (all suites green) and confirm in the preview the scenarios `sin-datos` and `caido` (quickstart §2): no houses, the fixed block shows its honest message, the rest of the page (roadmap, loan types, comparator) works and no console error stops other scripts.

**Checkpoint**: all four stories work; the feature is complete and still invisible to the public.

---

## Phase 7: Polish & cross-cutting concerns

**Purpose**: documentation, contract cross-references, final checks. No launch here.

- [ ] T038 [P] Append a Spanish section "Las casas flotantes de Comprar casa" to `INSTRUCCIONES-TASAS.md` (plain, verifiable steps, same tone as the file): what the two houses are and that they reuse the block's data and the same two switches (`TASAS_LANZADO` and `tasas_config.activo`) — no extra switch; the new public field `ultimaRevisionEn` and why it exists (so "Activo" is only shown when the agent really checked in the last 48 h); how to approve the lender list (open each link in a browser, confirm it is the lender's own public rates page, no referral parameters, then fill `PRESTAMISTAS` in `tasas-texto.js`; the launch guard test refuses `TASAS_LANZADO = true` with an empty list); how to preview locally (`node tests/preview-casas.js`, the `?caso=` scenarios and `?refresco=`); how to hide the houses without a deploy (`tasas_config.activo = false`); reminder that the site says nothing about the houses until the launch commit. Do not create a new file (the existing 404 rule for `INSTRUCCIONES-TASAS.md` already covers it).
- [ ] T039 [P] Add one line at the top of `specs/004-mortgage-rate-agent/contracts/snapshot-public.md` pointing to `specs/006-floating-rate-houses/contracts/snapshot-additive.md` ("Amended by feature 006: adds `ultimaRevisionEn`"); change nothing else in that file.
- [ ] T040 Run the full suite `node --test tests/` and report the exact result (all suites, including the untouched ones such as `zyron-leyes`, `cartas-bilingues`, `direccion-*`).
- [ ] T041 [P] Confirm LF endings on every edited file: `grep -c $'\r'` returns `0` for `tasas-hipoteca.js`, `tasas-texto.js`, `comprar-casa.html`, `netlify/functions/tasas-hipoteca.js`, `netlify/functions/tasas-hipoteca-logica.js`, `INSTRUCCIONES-TASAS.md`, the edited tests and the new `tests/preview-casas.js` and fixture; also confirm `netlify.toml` is unchanged (`git diff --stat netlify.toml` shows no change from this feature) and its CSP line was not touched.
- [ ] T042 Run `graphify update .` (constitution: Grafo de conocimiento).
- [ ] T043 Manually verify [quickstart.md](./quickstart.md) sections 1–3 end to end in the preview at 360 px, 768 px, 1280 px and phone landscape: all seven `?caso=` scenarios, and all 14 items of the §3 table; note any deviation instead of hiding it. Also confirm the network tab shows **one** snapshot request on load and no request to any other origin, and that with `TASAS_LANZADO` at its committed `false` value (open the page from a plain static server, not the preview) nothing is drawn and **no request** is made.

### Owner actions (not part of the implementation tasks; do not perform until the owner says so)

- [ ] T044 Owner (**G-LENDERS**): approve the lender list, open each link by hand, and give the final `nombre` + `url` entries to fill `PRESTAMISTAS` in `tasas-texto.js` (T030 ships it empty on purpose). Confirm the wording of the panel, and confirm placement and size on a real phone (**G-PLACE**). Feature 004's own owner actions (T055–T056: source list approval, SQL, first run) still apply.

### Launch commit (rides inside feature 004's launch commit T057–T059; perform only after the owner approves T044 and 004's T055–T056; in this order)

- [ ] T045 Edit `privacidad.html`: in the same short Spanish line that 004's T057 adds about the rate notice, also say that if you hide the mortgage-rate houses the site remembers it **only during that visit, in your own browser**, sends nothing and uses no cookies; the sentence MUST contain the exact phrase "ocultar las casas de tasas" (the T034 guard checks it). (**G-PRIV**)
- [ ] T046 With 004's T058 (search entry) and T059 (`const TASAS_LANZADO = true;`) done, deploy, then set `tasas_config.activo = true`. Check on the live site: fresh state, houses agree with the block, the panel opens with the approved lender links, and Umami shows only the categorical events `tasas-casa-vista`, `tasas-casa-abierta`, `tasas-casas-cerradas`, `tasas-prestamista-abierto`. Rollback: set `tasas_config.activo = false` (no deploy needed) or `TASAS_LANZADO = false`.

---

## Dependencies & execution order

- **Phase 1 → Phase 2 → stories.** Phase 2 blocks everything. Inside Phase 2: T004 depends on T003; T007 is independent of T005/T006; T005 before T006.
- **US1** (Phase 3) depends only on Phase 2: it is the MVP. Inside it: T008–T010 (tests, parallel) → T011 → T012 → T013 → T014 → T015 (T013 and T014 edit the same file, so they are sequential).
- **US2** depends on US1 (extends `pintarCasas`, `textoCasa` and the harness cases). Inside it: T016–T019 (tests, parallel) → T020 → T021 → T022 → T023 (parallel with T022/T024 — different file) → T024 → T025 → T026. T021 needs T020.
- **US3** depends on US2 (the panel shows `ultimaRevisionEn` and alerts). Inside it: T027–T029 (tests, parallel) → T030 → T031 (parallel with T030, different file) → T032 → T033.
- **US4** depends on US1–US3 having a single render path in place; T034/T035 are written first and T036 finishes the refactor.
- **Phase 7** after the stories you decide to ship; owner actions and the launch commit come last and are not done by these tasks.

## Parallel opportunities

- Phase 1: T001 ‖ T002.
- Phase 2: T003 ‖ T005 ‖ T007 (three different files); then T004 (after T003) ‖ T006 (after T005).
- US1 tests: T008 ‖ T009 ‖ T010 (three test files).
- US2 tests: T016 ‖ T017 ‖ T018 ‖ T019 (four test files); implementation: T022 ‖ T023 after T021.
- US3 tests: T027 ‖ T028 ‖ T029; implementation: T030 ‖ T031.
- US4 tests: T034 ‖ T035.
- Polish: T038 ‖ T039 ‖ T041.

Example — starting US2 in parallel after US1 is done:

```text
T016 tests/tasas-hipoteca-logica.test.js   T017 tests/tasas-hipoteca-funcion.test.js
T018 tests/tasas-texto.test.js             T019 tests/tasas-navegador.test.js
```

## Implementation strategy

- **MVP = Phase 1 + Phase 2 + US1.** Two houses with visible rates, in place, hideable and dismissible, reading the existing snapshot — already useful, and testable in the preview. Nothing is public because `TASAS_LANZADO` stays `false`.
- **Then US2 immediately**: it makes the houses *honest* ("Activo" only with proof, "Sin actualizar" otherwise, alert mark, live refresh) and carries the only server change. Do not launch without it.
- **Then US3** (details panel + lender links), **US4** (single render path and gate guards), and **Phase 7**.
- Each story ends with a full `node --test tests/` run and a preview check before the next begins; commit per story (constitution: cambios por partes pequeñas) — but only when the owner asks for a commit.
- Launch is not part of these tasks: it happens inside 004's launch commit after the owner completes T044 and 004's T055–T056.
